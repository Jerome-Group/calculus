# Map

Calculus: an interactive mathematics learning site.

Start here: `README.md`, then `AGENTS.md`.

| Area                              | Entry point                                                   |
| --------------------------------- | ------------------------------------------------------------- |
| Setup and public boundary         | `README.md`, `AGENTS.md`                                      |
| Routes, metadata, styles          | `app/`                                                        |
| Interactive lessons and controls  | `components/`                                                 |
| Mathematics and shared helpers    | `lib/`, `hooks/`                                              |
| Static and vendored assets        | `public/`, `vendor/`                                          |
| Worker and build integration      | `worker/`, `build/`, `vite.config.ts`                         |
| Original Sites identity           | `.openai/hosting.json`                                        |
| Retained database scaffolding     | `db/`, `drizzle/`, `drizzle.config.ts`                        |
| Build helpers and tests           | `scripts/`, `tests/`                                          |
| Formatting and lint debt          | `package.json`, `.prettierignore`, `eslint-suppressions.json` |
| CI and conformance                | `.github/`                                                    |
| Domain, decisions, agent routines | `CONTEXT.md`, `docs/`                                         |
| Examples                          | `examples/`                                                   |

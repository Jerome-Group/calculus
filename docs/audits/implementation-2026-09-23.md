# 22 September re-audit implementation evidence

Status: implementation and local browser review in progress. This record distinguishes observation from source inference. It is not a claim that all 125 lessons have been substantively audited or that production has been updated.

## Deployment identity before release

On 23 September 2026 the Sites API reported the existing public **Calculus** Site at `https://calculus.jeromegroup.org`, version 4, saved from commit `d5977e8745563b8d36778bbf6860ff3c40b7a206`, with its deployment succeeded on 15 September 2026. This is the audit snapshot, older than the GitHub default branch when this implementation began (`84db05c3fd45cd65d310e22b7f2e92cd0df1457f`). Local branch behavior below must not be attributed to that production deployment. The existing Site project ID remains in `.openai/hosting.json`.

The same public URL loaded in the in-app browser during this review. Its course overview and the previous 14 WebMCP tools were visible. This confirms availability of the old deployment, not the unpublished branch.

## Local executable checks

Node 24.11.0; `npx vinext build`, all 78 `node --test tests/*.test.mjs`, `npm run sources:check`, `npm run format:check`, and `npm run lint` passed during implementation. A final run against the committed PR head is still required. The Vinext build reports advisory Vite native-config and chunk-size warnings. Standalone `tsc --noEmit` still reports Cloudflare ambient types already present before these changes; Vinext builds successfully.

## Local browser observations

Tests used the production build served at `http://127.0.0.1:4173/` in the Codex in-app browser on 23 September 2026. Exact browser version was not reported by that surface. A separate headless Chrome 153.0.8010.53 run measured the [before/after visual hierarchy](visual-hierarchy-2026-09-23.md) at 320px, 390px and 1440px; its six [captures](browser-captures/) are retained. These are observed local results, not production results.

| Case                      | Viewport and action                                                                                                         | Observation                                                                                                                                      | Limit                                                                                                      |
| ------------------------- | --------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------- |
| Stable routes             | WebMCP `list_concepts` returned 125 IDs; `open_concept` visited each and checked its lesson heading                         | 125/125 had a heading; no route call failed                                                                                                      | Does not independently inspect every scene, source, formula or practice task                               |
| Math-first mobile         | 390×844 and 320×720, Rolle/MVT Learn                                                                                        | Question and objects/hypotheses occurred before the main interactive scene; the 320px document had equal scroll and client widths                | Other 124 lessons and true browser zoom need separate checking                                             |
| Revision                  | 320×720, Rolle/MVT Revise                                                                                                   | Theorem statements, hypothesis lists, proof statuses, failure case, practice and full-reasoning action rendered; no document horizontal overflow | Related-lesson mixed practice was added after this viewport check                                          |
| Interrupted study         | Total differentiability: set radius 1.1, type a distinct draft, follow a prerequisite and use Return                        | Original lesson and parameter 1.1 restored; exact answer text remained in the textarea                                                           | Automated locator timed out immediately after navigation; fresh DOM inspection confirmed the restored text |
| Independent inline WebMCP | Polar regions: set x to 1.37, reload, set radial angle to 0.6, follow the main prerequisite and return                      | Each control kept its value and matching readout; the visible radial slider read 0.6; return restored the lesson and both values                 | Device storage only; no cross-device sync                                                                  |
| Graph deep link           | WebMCP plotted `x^2+y^2-a` on [-2,2]² with a=1.25, then reloaded `#graph`                                                   | URL and reloaded WebMCP state retained the exact expression, bounds and parameter; browser error log was empty                                   | Does not validate all graph modes on all devices                                                           |
| Exact review WebMCP       | Total differentiability: set counterexample radius to 0.20 at diagonal angle while the other function stayed at radius 0.25 | WebMCP and visible readout both gave signed normalized remainder −0.7071; the two function states remained independent                           | A sampled mesh is illustrative; the exact residual proves the result                                       |
| Compatibility rendering   | 1280×800, total differentiability: choose compatibility view                                                                | SVG replaced canvas, carried `aria-describedby` for formula/note/readout, and browser error log was empty                                        | Context-loss and assistive-technology tests remain                                                         |

The [focused keyboard and reduced-motion check](ca016-browser-qa-2026-09-23.md) records cap orientation, SVG mode, focus outline and a repaired motion-preference gap. Error logs were empty in the in-app compatibility check. The screenshot files show public lesson content only.

## Remaining validation

The core source ledger explicitly records 120 topic-unit decisions, with 28 atomic outcome candidates in 23 units and topic-specific gaps in the other 97. All 28 cited physical PDF pages were checked against extracted source text. No outcome is marked independently checked or complete; rendered outcome checks and learner testing are pending. MH2100 annotated Lectures 02–05 are image-only in the available text extraction and remain unresolved. Actual 200%/400% zoom, screen-reader reading, contrast measurement, lower-powered-device performance, and formative learner transfer have not been observed here. Production browser verification follows the final merge and publish.

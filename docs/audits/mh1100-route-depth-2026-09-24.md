# MH1100 route depth review · 24 September 2026

Scope: all 24 public MH1100 concept IDs at `574faeb`. I compared each route's source citations and atomic outcome IDs with its prerequisites, theorem labels, worked blocks, changed-data exercises, feedback, and scene assignment. Counts alone are not learner performance evidence. The guide and ledger checks are reproducible in `tests/mh1100-route-depth.test.mjs`; source-section page review is recorded in the MH1100 lecture mapping tests and ledger. This review did not observe a learner using a route.

Every row below has at least one source citation and mapped atomic outcome, a worked argument with a check, and independent practice with hints, solutions, and a rubric. “Keep” means the route's displayed instructional depth supports its mapped claims; it does not certify a learner or claim that its scene proves an outcome. Each scene is a visual candidate until its exact state is aligned to that outcome. The text argument and feedback carry the mathematical claim.

| Route | Decision and checked distinction |
| --- | --- |
| `sets-functions-domains` | Keep. Set membership, declared domain/codomain/range, and the vertical-line test are separate. Domain and function checks use changed sets and rules. |
| `function-shapes` | Keep. Piecewise endpoint ownership, even/odd tests, and family domains are worked separately; practice changes branches and inputs. |
| `transform-compose` | Keep. Point mapping and composition order are distinct, with domain restrictions checked before evaluation. |
| `limits-one-sided` | Keep. Two-sided limits require matching sides; a point value and a missing value are not substituted for nearby behavior. |
| `infinite-limits` | Keep. The sign of each approach side and threshold meaning of infinity are explicit; asymptote claims use those limits. |
| `limit-laws-squeeze` | Keep. Denominator/root eligibility and bounding estimates precede limit laws; the radian trigonometric limit has its own argument. |
| `epsilon-delta-one-variable` | Keep. Quantifier order, local bounds, and one fixed epsilon to disprove a limit appear in reasoning and transfer. |
| `continuity-ivt` | Keep. Domain-relative continuity and endpoint hypotheses are checked; IVT yields existence, never uniqueness by itself. |
| `derivative-definition` | Keep. Nonzero secants and finite limits lead to tangent rate; the quotient is simplified before setting the increment to zero. |
| `differentiability-corners` | Repair proof label. The statement and following worked block prove differentiability implies continuity; the label incorrectly said no proof. Corner and jump counterexamples still distinguish the converse. |
| `differentiation-rules` | Keep. Product/quotient derivations, original domains, and radian trigonometric rules confront tempting invalid shortcuts. |
| `chain-rule-single` | Keep. Differentiability remainders justify the chain rule, including zero inner increments; nested domains are checked. |
| `implicit-related-rates` | Keep. A local branch must exist before solving for its derivative; rate equations retain units and constraints. The generic curve scene is illustrative. |
| `linearization-differentials` | Keep. Approximation and equality, little-o versus big-O error, and tangent bias are distinguished in worked and transfer tasks. |
| `extrema-fermat` | Repair proof label. The following one-sided quotient argument proves Fermat's conclusion under interior differentiability; candidates still require endpoint and value comparison. |
| `rolle-mean-value` | Keep. Continuity and differentiability hypotheses are explicit; the secant subtraction proof and derivative-zero consequence are distinct. |
| `derivative-tests` | Keep. First and second derivative tests retain continuity and sign hypotheses; a zero second derivative is inconclusive. Its scene cannot establish exact sign charts or nondifferentiable candidates. |
| `infinity-curve-sketching` | Keep. Both tails, vertical/slant behavior, domain components, and derivative signs are assembled analytically. Its scene illustrates one positive-tail horizontal case only. |
| `optimization-single` | Keep. Feasible domain, objective, critical points, and endpoint comparison justify the global conclusion; a graph alone does not. |
| `newton-method` | Keep. The tangent update requires nonzero derivative and has a failure/cycle example; a plotted root is an estimate until checked. |
| `antiderivatives-single` | Keep. Constants vary independently on disconnected components; initial values and physical time restrictions are applied after integration. |
| `inverse-functions` | Keep. Injectivity and inverse existence precede reciprocal-slope differentiation; the zero-derivative case is an explicit warning. |
| `exponential-logarithmic` | Keep. Positive base and logarithm domains, inverse identities, and derivative foundations precede log differentiation; an unproved growth comparison is named as such. |
| `lhopital` | Keep. Eligible indeterminate forms, one-sided hypotheses, Cauchy bridge, and product conversion are differentiated from direct limit methods. The scene is a candidate only. |

The two proof-label corrections are limited to displayed theorem status text. No atomic outcome was promoted to mastery or scene-verified. Production rendering and WebMCP state checks belong to release issue #30 and must be repeated against the final published build.

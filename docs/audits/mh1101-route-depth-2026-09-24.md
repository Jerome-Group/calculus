# MH1101 route depth review · 24 September 2026

Scope: the 30 public MH1101 concept IDs. I checked each route's source citation and atomic outcome against prerequisites, theorem conditions/proof status, exact worked reasoning, changed-data practice and feedback, method/failure contrasts, and scene claims. The automated structural guard is `tests/mh1101-route-depth.test.mjs`; source-page calculations have separate chapter tests. A guide, scene, or test does not establish learner mastery. No learner session was run in this review.

The route decisions below distinguish checked instructional claims from learner-performance claims. “Keep” means the displayed lesson supports its mapped claim; every scene remains an illustration or candidate unless its exact state is aligned to that outcome. Worked text and feedback carry the mathematical conclusion.

| Route | Decision and checked distinction |
| --- | --- |
| `integral-antiderivatives` | Keep. Antiderivatives form a family with constants chosen per connected domain component; reversing a chain rule requires its inner derivative. |
| `riemann-integral` | Keep. Tagged partitions and limiting sums define signed accumulation; interval orientation and additivity are checked separately. |
| `integral-average` | Keep. Integral average divides by interval length; continuity is needed to guarantee an attaining point. A discontinuous counterexample separates average from attainment. |
| `fundamental-theorem` | Keep. Accumulation differentiation and antiderivative evaluation have different jobs; moving bounds require chain-rule factors and orientation. |
| `substitution-integral` | Keep. The differential, integrand, and bounds move together; a decreasing substitution reverses orientation. |
| `improper-integrals` | Keep. Each improper endpoint has its own limit; cancellation across divergent pieces cannot create a conventional integral. |
| `area-between-curves` | Keep. Upper-minus-lower height is nonnegative only on intervals with that order; split at crossings or change slice direction. |
| `disk-washer-volumes` | Keep after #162. The actual section shape, axis-relative radii, horizontal/vertical slices, and squared-radius subtraction are worked separately. |
| `cylindrical-shells` | Keep after #162. A strip parallel to the axis produces circumference times height; examples contrast cubic inversion, horizontal shells, and between-curve heights. |
| `integration-by-parts` | Keep after #164. The product-rule formula keeps the boundary term in definite form; source examples cover ordinary, cyclic, and reduction cases. LIATE is marked a heuristic, and changed-data tasks test factor choice and recurrence. |
| `trigonometric-integrals` | Keep after #164. Odd sine/cosine powers, even half-angle reductions, tangent/secant parity, and different-frequency product-to-sum each have source work and independent transfer. The saved-factor choice differs by parity. |
| `trigonometric-substitution` | Keep after #164. Radical pattern, scale, and one-to-one trigonometric branch are explicit; source sine/tangent examples and a guide-labelled ellipse solution keep source proof status honest. |
| `partial-fractions` | Keep after #164. Properness/division precedes decomposition; distinct linear factors and repeated irreducible quadratics use complete templates. Source coefficient work and a guide-labelled unsolved-prompt extension are distinguished. |
| `quadrature-midpoint-trapezoid` | Keep after #164. Midpoint and trapezoid node choices, signed-error convention, and second-derivative bounds are separate. Source estimates are checked against independent polynomial and guarantee exercises. |
| `simpson-rule` | Keep after #164. Composite endpoint/alternating weights require even panels; the fourth-derivative error bound and smallest-even-panel guarantee have source examples and changed-data checks. |
| `sequence-limits` | Keep. The index start and quantified tail bound are distinct; error tolerances produce a concrete cutoff. |
| `subsequences-bounded` | Keep. A genuine limit passes to subsequences, while boundedness alone does not imply one; divergent lower bounds and parity examples are checked. |
| `sequence-limit-laws` | Keep. Finite component limits and nonzero denominator conditions precede algebra; squeeze and growth comparisons use their hypotheses. |
| `monotone-sequences` | Keep. Invariant bounds and monotonicity establish convergence before continuity identifies a recursive limit; the completeness theorem itself is marked unproved. |
| `series-geometric-telescoping` | Keep. The series is a limit of finite partial sums; geometric and telescoping formulas retain their surviving terms and convergence condition. |
| `series-divergence-test` | Keep. A nonzero term limit proves divergence; a zero term limit is inconclusive, as the harmonic block argument demonstrates. |
| `integral-test` | Keep. Positivity/decrease hypotheses, p-series thresholds, and quantitative tail bounds are distinct from merely integrating a formula. |
| `comparison-tests` | Keep. Direct comparison uses the inequality in the correct direction; limit comparison needs a positive finite ratio or its stated edge case. |
| `absolute-conditional-alternating` | Repair proof label. The guide proves absolute convergence implies convergence by comparing nonnegative terms, so its earlier “without proof” label contradicted the next block. Alternating and absolute verdicts remain separate. |
| `ratio-root-tests` | Keep. Factorials and powers suggest different tests; ratio/root limit one is explicitly inconclusive and needs another method. |
| `power-series-radius` | Keep. The radius controls the open interval only; endpoints are separate series and a shifted center changes the interval. |
| `power-series-operations` | Keep. Termwise operations stay inside the radius; endpoint behavior may change even when the radius does not. |
| `taylor-series` | Keep. Coefficients, series convergence, and equality to the function are separate; a remainder estimate, including at a nonzero center, supports equality. |
| `binomial-series` | Keep. Generalized coefficients and the open radius are explicit; terminating and nonterminating cases do not share an automatic endpoint verdict. |
| `limits-with-series` | Keep. The expansion retains enough terms after cancellation; the source prompt without a supplied solution is not presented as a source solution. |

The Chapter 03 decisions were checked against the canonical 35-page notes and the 18 outcomes added in #164. Its three “Give it a try” prompts without source solutions are labelled as guide solutions. The proof-label correction changes only theorem status text. No route is promoted to outcome mastery, and no source PDF or private learning record is committed.

# Production browser evidence · 2026-09-23

Issue #30. The public Site is `https://calculus.jeromegroup.org`. Initial captures came from version 8, deployed from `c9be96187f0b0b2799a27757136e49266fb7b815`. They used the Codex In-app Browser (Chromium family; exact version unavailable) on macOS. The tested viewports below are CSS pixels; browser device-scale override was not used. Every capture was observed after hydration. [Console events](browser-captures/production-v8-console.json) were empty in the production test tab. No Site worker errors appeared in the 30-minute post-deployment query.

| Capture                                                                                                  | State, input, expected result                                                                                                                             | Observed result                                                                                                                                                            | Verdict                              |
| -------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------ |
| [Desktop graph](browser-captures/production-v8-graph-desktop.jpg), 1280×720, WebGL                       | Open the original shared `x^2+y^2-a` URL with `a=1.25`; the quick equation should typeset and the graph should be visible.                                | Quick bar contained two KaTeX expressions, including `z=x²+y²−a`; no raw quick input or KaTeX error. WebMCP `get_study_state` returned the exact expression and parameter. | Pass                                 |
| [Mobile practice](browser-captures/production-v8-practice-mobile-390.jpg), 390×844, text input           | Open epsilon-delta Practice and type a setup; text and choices should remain reachable without document-level horizontal scroll.                          | Setup and transfer were visible; the typed derivation appeared, with `scrollWidth=390`. This version lost the pilot draft on lesson navigation; filed as #38.              | Layout pass; state defect            |
| [Desktop fallback](browser-captures/production-v8-flux-fallback-desktop.jpg), 1280×800, SVG              | Open flux lesson and toggle compatibility rendering; formula, current readout and scene description should remain.                                        | One accessible `svg[role=img]` replaced the WebGL scene; current formula/readout remained; zero KaTeX errors.                                                              | Pass for explicit fallback           |
| [Local restored draft](browser-captures/local-pilot-draft-restored-desktop.jpg), 1280×720, keyboard/text | On the #38 branch, select `δ=0.2`, type a distinct setup, navigate to a different lesson, use Back, then reload. The choice and derivation should return. | Both returned after Back and reload, with “Draft restored from this device.” Task storage was isolated in a denial/clear regression.                                       | Local pass; production v9 pass below |

After that capture, the local test selected the incorrect setup choice `δ=0.4`, checked it, then deleted only that task's draft. Revise still listed `linear: BOUND_TOO_LARGE · no hint`; returning to Practice showed an empty setup while the separate transfer derivation remained. Switching Learn → Practice also preserved the transfer derivation. The draft storage test covers denied writes, an invalid cold record, and independent task deletion.

The public Site's WebMCP `list_concepts` returned 125; `open_concept` visited all 125 Learn pages. No visible raw math markers or KaTeX errors appeared. `configure_graph` changed the surface expression and restored it, with no browser console errors. The initial local full sweep also covered 375 Explore/Practice/Revise states.

## Merged release recheck

Version 9 was published from merged commit `9fd16f4670dc8edc4466ebc245219c0565a30d4a` after PR #39 and with no open Dependabot PRs. In a fresh production session, the original shared graph URL rendered its quick expression with KaTeX and retained `a=1.25`; WebMCP `get_study_state` returned the same values. On epsilon-delta Practice, a selected choice and typed setup survived Learn → Practice, next concept → Back, and reload. The test draft was deleted afterward. WebMCP `list_concepts` and `get_study_state` still worked; the pilot page exposed 18 MathML nodes and no KaTeX error. The browser warning/error console was empty. The Site worker error-only query found no application exception; it did include one screenshot-bot request for `/favicon.ico` returning 404, while the configured PNG icon exists.

Actual Chrome page zoom was tested on macOS with the browser zoom shortcut, then reset to 100%. Chrome's exact version was not exposed; baseline device-pixel ratio was 2. At 200%, it was 4 with CSS viewport and document width both 864px. At 400%, it was 8 with CSS viewport and document width both 432px. The text and task controls reflowed and remained reachable, including the transfer task, and there were no KaTeX errors. The screenshots show [200% Practice](browser-captures/production-v9-practice-zoom-200.jpg) and [400% transfer](browser-captures/production-v9-practice-zoom-400.jpg). These observations cover the pilot page, not every long formula across the 125 routes.

The browser checks do not establish spoken screen-reader output, automatic WebGL context-loss fallback, graphics contrast in every state, or lower-powered-device performance. The explicit SVG compatibility path passed. Actual keyboard navigation, MathML exposure, text contrast measurements and reduced-motion behavior were checked in the earlier production browser record on issue #30. Unfamiliar learner sessions were not run.

## Narrow formula overflow follow-up

On the #42 branch, Chrome DevTools device emulation set a 320×700 CSS viewport at device scale 2 on the epsilon-delta Practice lesson. The long working relation measured 382px inside a 280px local formula region, while the document remained exactly 320px wide. The region now has a descriptive accessibility name, retains MathML, and has a visible focus outline. Keyboard focus plus ArrowRight moved its local `scrollLeft` from 0 to the 102px maximum, revealing the end of the equation; [the capture](browser-captures/local-formula-keyboard-320.jpg) shows that state. No KaTeX errors appeared. At a 640px CSS viewport, the relation fit its 600px region and the page stayed 640px wide; at the normal desktop viewport, both page and document width were 1728px, the named region and MathML remained. The 640px and 320px emulated widths correspond to the reflow space of 200% and 400% zoom on a 1280px layout, but they are not a second actual browser-zoom test. The rendered markup has a regression test for named keyboard focus and MathML. This is local branch evidence; production verification follows merge and publish. WebMCP `get_study_state` and `list_concepts` remained available on the branch.
